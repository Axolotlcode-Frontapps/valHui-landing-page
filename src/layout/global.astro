---
import "@/ui/styles/global.css";

import { config } from "@/lib/utils/config";
import { Footer } from "@/ui/shared/footer";
import { Header } from "@/ui/shared/header";
import { Toaster } from "@/ui/shared/sonner";

interface Props {
	title?: string;
	description?: string;
}

const language = Astro.currentLocale ?? "es-MX";

const {
	title = config.brand,
	description = "Plataforma líder en la renta de espacios comerciales y profesionales en México, conectando negocios con los lugares ideales para crecer.",
} = Astro.props;

const seoTitle = title === config.brand ? title : `${title} | ${config.brand}`;
---

<html lang={language}>
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{seoTitle}</title>
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
    <Toaster client:load />
  </body>
</html>
